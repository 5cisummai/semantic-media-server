# Important notes

- _Do not_ install any pnpm packages to the project root, only put them in the app directory

- _Do not_ create a root package json

- _Never_ add a .pnpm-store and instead use the command line to `pnpm install` a package from there

- When installing in pnpm first `cd app` then `pnpm install`
